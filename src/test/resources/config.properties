# Environment config for the ACME store E2E suite.
# Override at the command line: -Dbrowser=firefox (browser only; the rest
# is edit-this-file, the 2019 way).
base.url=https://shop.example.com
browser=chrome
timeout.seconds=10
screenshot.dir=target/screenshots

# Seeded QA account (staging only — resets nightly with 23 orders)
default.user=a@b.com
default.password=correct-horse
